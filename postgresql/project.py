"""
Project information.
"""

name = 'py-postgresql-og'
identity = 'http://github.com/vimiix/py-postgresql-og'

meaculpa = 'Python+Postgres+openGauss'
abstract = 'Driver and tools library for PostgreSQL and openGauss'

version_info = (1, 3, 1)
version = '.'.join(map(str, version_info))
